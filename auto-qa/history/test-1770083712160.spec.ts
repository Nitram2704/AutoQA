on
{
  "testName": "Google Search DeepMind",
  "imports": "import { test, expect } from '@playwright/test';",
  "testCode": "test('Search Google for DeepMind', async ({ page }) => {\n  await page.goto('https://www.google.com');\n\n  // Locate the search input and type 'DeepMind'\n  const searchInput = page.getByRole('combobox', { name: 'Search' });\n  await searchInput.fill('DeepMind');\n\n  // Press Enter (or use the search button if available)\n  await searchInput.press('Enter');\n\n  // Assert that the search results page loads (check for title containing search query)\n  await expect(page).toHaveTitle(/DeepMind - Google Search/);\n\n  // Optional: Check for a specific result (example).\n  const firstResult = page.getByRole('heading', { name: /DeepMind/i, level: 3 }); //Example selector, adjust as needed\n  await expect(firstResult).toBeVisible();\n});",
  "fullScript": "import { test, expect } from '@playwright/test';\n\ntest('Search Google for DeepMind', async ({ page }) => {\n  await page.goto('https://www.google.com');\n\n  // Locate the search input and type 'DeepMind'\n  const searchInput = page.getByRole('combobox', { name: 'Search' });\n  await searchInput.fill('DeepMind');\n\n  // Press Enter (or use the search button if available)\n  await searchInput.press('Enter');\n\n  // Assert that the search results page loads (check for title containing search query)\n  await expect(page).toHaveTitle(/DeepMind - Google Search/);\n\n  // Optional: Check for a specific result (example).\n  const firstResult = page.getByRole('heading', { name: /DeepMind/i, level: 3 }); //Example selector, adjust as needed\n  await expect(firstResult).toBeVisible();\n});"
}